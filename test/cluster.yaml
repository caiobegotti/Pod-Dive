# testing cluster config to verify commands work:
# kind create cluster --config=cluster.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kind-test-cluster
nodes:
- role: control-plane
- role: worker  
