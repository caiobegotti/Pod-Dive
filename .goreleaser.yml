project_name: nodes-dive
release:
  github:
    owner: caiobegotti
    name: nodes-dive
builds:
  - id: nodes-dive
    goos:
    - linux
    - windows
    - darwin
    goarch:
    - amd64
    - "386"
    env:
      - CGO_ENABLED=0
      - GO111MODULE=on
    main: cmd/plugin/main.go
    ldflags: -s -w
      -X github.com/caiobegotti/nodes-dive/pkg/version.version=
archives:
  - id: nodes-dive
    builds:
    - nodes-dive
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
    - goos: windows
      format: zip
