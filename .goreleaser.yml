project_name: krew-nodes-dive
release:
  github:
    owner: caiobegotti
    name: krew-nodes-dive
builds:
  - id: krew-nodes-dive
    goos:
    - linux
    - windows
    - darwin
    goarch:
    - amd64
    - "386"
    env:
      - CGO_ENABLED=0
      - GO111MODULE=on
    main: cmd/plugin/main.go
    ldflags: -s -w
      -X github.com/caiobegotti/krew-nodes-dive/pkg/version.version=
archives:
  - id: krew-nodes-dive
    builds:
    - krew-nodes-dive
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
    - goos: windows
      format: zip
